plugins {
    id "org.gradle.toolchains.foojay-resolver-convention" version "1.0.0"
}

rootProject.name = "applied-history"

// use the published ae2 maven artifact (includes client classes). local includeBuild of ae2
// only exposes the common source set and breaks client compilation for this mod.

